{
  "name": "bot-forms-sync",
  "version": "1.0.0",
  "private": true,
  "description": "Bot que sincroniza evidências de inspeção das obras Novo Nordisk com o forms SAR (Microsoft Forms) do cliente.",
  "scripts": {
    "sync": "tsx sync.ts",
    "install-browsers": "playwright install chromium"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "playwright": "^1.47.0"
  },
  "devDependencies": {
    "tsx": "^4.19.1",
    "typescript": "^5.4.5",
    "@types/node": "^20.12.7"
  }
}
